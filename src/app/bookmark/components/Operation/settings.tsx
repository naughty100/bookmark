'use client';
import React from 'react';

// 使用Record<string, never>替代空接口，或添加实际需要的属性
// 移除未使用的类型定义

export default function Settings() {
  return (
    <div className="space-y-4">
      {/* 全局设置的配置项，后续开发 */}
    </div>
  );
}