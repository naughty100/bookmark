'use client';
import React from 'react';

interface BackgroundOpProps {
  // 后续添加工作区背景相关的配置
}

export default function BackgroundOp({}: BackgroundOpProps) {
  return (
    <div className="space-y-4">
      {/* 工作区背景配置，后续开发 */}
    </div>
  );
}